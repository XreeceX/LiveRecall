# Defaults are fine for a hackathon-scale app. LiveKit and OkHttp ship
# their own consumer rules so we don't need to repeat them here. Add
# project-specific keep rules below if release builds start dropping
# something at runtime.
