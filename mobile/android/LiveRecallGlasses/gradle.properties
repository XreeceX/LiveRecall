org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.enableJetifier=false
kotlin.code.style=official

# Meta Wearables Device Access Toolkit credentials (fill in once you
# uncomment the maven repo in settings.gradle.kts). Keep these out of
# git — set them in ~/.gradle/gradle.properties or via -P.
# META_USERNAME=
# META_TOKEN=
