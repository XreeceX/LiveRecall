fastapi==0.115.4
uvicorn[standard]==0.32.0
python-dotenv==1.0.1
# Pydantic 2.11+ triggers forward-ref errors when constructing ChatOpenAI
# (Vision /snap path). Keep below 2.11 until langchain-openai catches up.
pydantic>=2.7.4,<2.11

# Mongo
motor==3.7.1
pymongo==4.10.1

# LangChain + agents
langchain-core==0.3.21
langchain-openai==0.2.10
langchain-mongodb==0.4.0
langgraph==0.2.50
openai==1.55.3

# LiveKit (unpinned — tight inter-package constraints, let pip resolve)
livekit>=0.18.1
livekit-agents>=0.12.16,<1.0
livekit-api>=0.8.0
livekit-plugins-elevenlabs>=0.7.10,<1.0
livekit-plugins-openai>=0.10.10,<1.0
livekit-plugins-silero>=0.7.4,<1.0

# Voice I/O — single vendor: ElevenLabs handles both STT (Scribe v2 Realtime,
# direct websocket in backend/stt.py) and TTS (Flash v2.5, direct websocket in
# backend/tts.py). See DECISIONS.md.
elevenlabs==1.13.1

# Imaging
Pillow==11.0.0
numpy==2.1.3
mss==10.0.0              # bridge_rayban_snap: cross-platform screen capture (Windows, macOS, Linux)

# Meta Ray-Ban bridge (scripts/bridge_rayban.py) — decodes MP4 H.264 + AAC
# and republishes synced audio+video into a LiveKit room as the "glasses"
# participant. Only needed to run the bridge script; the FastAPI server +
# agents themselves do not import `av`.
av>=13.0

# Misc
httpx==0.27.2
websockets==13.1
orjson==3.10.11

# Dataset ingest (scripts/ingest_*.py). Only needed if you re-run the ingest
# pipeline; the committed fixtures under data/fixtures/ work without these.
pyarrow==18.0.0    # parses the MTSamples parquet from Hugging Face
